apiVersion: k8s.patoarvizu.dev/v1alpha1
kind: KMSVaultSecret
metadata:
  name: demo-app
  namespace: default
spec:
  path: secret/demo-app
  kvSettings:
    engineVersion: v1
  secrets:
  - key: hello
    encryptedSecret: 